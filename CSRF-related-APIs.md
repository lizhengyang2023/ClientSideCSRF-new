# Web安全-CSRF分析

# 论文内容

## 可能导致客户端CSRF的接口

异步请求
推送请求
服务器发送事件
WebSocket
顶级导航请求
窗口导航请求

## 相关接口的作用与风险

### 异步请求（Async）

- 风险：劫持请求目标/参数，冒用身份执行操作或窃取数据。

种类：

`XMLHttpRequest` 

早期的异步请求API，比fetch繁琐

- **功能说明**：这是最早的浏览器端异步网络请求 API。它是一个对象，通过监听其事件（如 `onload`, `onerror`, `onprogress`）来处理请求的不同阶段。
- **核心特点**：成熟稳定，所有浏览器都支持，提供了细致的状态控制。但 API 设计相对陈旧，使用起来比 `fetch` 繁琐。
- **典型应用场景**：
    - **兼容性要求高的项目**：需要支持非常古老的浏览器。
    - **需要上传进度的场景**：`XMLHttpRequest` 的 `progress` 事件可以方便地实现文件上传进度条。
    - **部分库的内部实现**：一些旧的 JavaScript 库可能仍在内部使用 XHR。

`fetch` 

现代异步请求API，支持async、await语法；

- **功能说明**：`fetch` 是现代 JavaScript 中用于发起网络请求的标准 API。它返回一个 Promise，使用起来比 `XMLHttpRequest` 更简洁、更现代，支持异步 / 等待（async/await）语法，让代码更易读。
- **核心特点**：基于 Promise，支持流式处理响应（Response），默认不发送 cookies，也不会拒绝 HTTP 错误状态码（如 404、500）。
- **典型应用场景**：
    - **数据获取**：从后端 API 获取 JSON 数据（如用户信息、文章列表、商品数据）并更新页面。
    - **表单提交**：将用户填写的表单数据（如评论、个人设置）异步提交到服务器。
    - **资源加载**：动态加载图片、脚本等资源。

`navigator.sendBeacon()` 

- **功能说明**：这个 API 专门用于向服务器发送少量数据，特别适合在页面卸载（unload）时发送统计信息、日志或分析数据。
- **核心特点**：它是异步的，但不会阻塞页面的卸载过程，比 `fetch` 或 `XHR` 在 `unload` 事件中更可靠，因为浏览器会尽力保证请求的发送。请求方法是 POST。
- **典型应用场景**：
    - **用户行为跟踪**：记录用户在页面上的停留时间、点击的按钮等。
    - **错误日志上报**：当页面发生 JavaScript 错误时，将错误信息发送到服务器。
    - **会话结束统计**：在用户关闭页面或跳转到其他页面时，发送本次会话的相关数据。

### 推送请求（Push）

- 风险：劫持订阅链接

```jsx
subscription = await registration.pushManager.subscribe({ // 用push管理器订阅消息源
                        userVisibleOnly: true,
                        applicationServerKey: applicationServerKey
                    });
```

- **功能说明**：一种浏览器 API，它允许 Web 应用在后台接收来自服务器的推送消息，即使应用当前没有打开在活跃的标签页中，是实现 “Web 推送通知” 的核心技术。（半双工-服务器向客户端）
- **核心特点**：
    - **异步与后台**：消息送达不依赖于页面是否处于活跃状态。
    - **用户授权**：需要用户明确授予网站发送通知的权限。
    - **依赖 Service Worker**：消息的接收和处理必须在 Service Worker 中完成，这是一个独立于网页主线程的后台脚本。
    - **通知展示**：通常与 Notification API 结合使用，在系统托盘或浏览器角落显示可视化通知。
- **典型应用场景**：
    - **即时通讯**：接收新消息通知（如微信、WhatsApp Web）。
    - **实时提醒**：接收邮件、日程、银行交易等提醒。
    - **内容更新**：博客、新闻网站推送最新文章。
    - **活动通知**：电商平台的促销活动、游戏中的任务完成提醒。

### **服务器发送事件（Event Source）**

- 风险：劫持订阅链接

```jsx
const eventSource = new EventSource('/api/sse');

eventSource.onopen = () => {
    console.log('SSE 连接已建立');
};

eventSource.onmessage = (event) => {
    console.log('收到 SSE 消息:', event.data);
    const data = JSON.parse(event.data);
    if (data.type === 'system_broadcast') {
        addMessage('📢 系统广播', data.message);
    }
};
```

- **功能说明**：SSE 是一种基于 HTTP 的单向通信协议，允许服务器向客户端（浏览器）持续发送数据流。客户端建立一个持久的 HTTP 连接，服务器通过这个连接不断推送数据。（服务器向客户端发送数据）
- **核心特点**：
    - **单向通信**：数据只能从服务器流向客户端。
    - **基于 HTTP**：使用标准的 HTTP/HTTPS 协议，无需特殊端口，易于穿透防火墙。
    - **文本流**：传输的数据格式通常是 UTF-8 编码的文本，可以是纯文本或 JSON。
    - **自动重连**：浏览器内置了断线自动重连机制。
    - **简单轻量**：API 简单，易于实现和使用。
- **典型应用场景**：
    - **实时数据更新**：股票行情、体育赛事比分、实时监控仪表盘。
    - **社交媒体动态**：实时显示新的点赞、评论、关注。
    - **通知流**：网站内的实时通知（如站内信、系统公告）。
    - **日志或数据推送**：服务器向客户端推送实时日志。

### WebSocket

- 风险：劫持订阅链接

```jsx
ws = new WebSocket(`ws://localhost:8080/ws?user=${userId}`); 

ws.onopen = () => {
    console.log('WebSocket 连接已建立');
    addMessage('系统', '已连接到聊天服务器');
};
```

- **功能说明**：WebSocket 是一种在单个 TCP 连接上进行**全双工（双向）通信的**协议。它在客户端和服务器之间建立一个持久的连接，允许双方随时发送数据。
- **核心特点**：
    - **全双工通信**：服务器和客户端都可以主动发送数据。
    - **基于 TCP**：独立于 HTTP，需要单独的握手过程来升级连接。
    - **低延迟**：连接建立后，数据传输的 overhead 很小。
    - **二进制和文本**：可以传输文本数据（如 JSON）和二进制数据（如图片、文件）。
- **典型应用场景**：
    - **即时通讯（IM）**：聊天应用（如 Discord, Slack）。
    - **多人在线游戏**：实时同步游戏状态、玩家操作。
    - **协作工具**：多人实时编辑文档、白板。
    - **实时交易系统**：金融领域的高频交易、实时报价。
    - **视频会议和直播互动**：实时弹幕、连麦状态同步。

### 顶层导航

`Location url`

- 风险：篡改导航目标，诱导至恶意页面或触发非预期操作
- `location` 是 `window` 对象的一个属性，它提供了**当前窗口**中加载的文档的 URL 信息，并且允许你通过脚本修改这个 URL，从而实现页面导航。
    1. **获取当前 URL 信息**：`location` 对象包含了 URL 的各个组成部分，你可以像读取普通对象属性一样获取它们。
    2. **修改 URL 进行导航**：通过修改 `location` 对象的某些属性（如 `href`），你可以让浏览器加载一个新的页面。
    3. **刷新当前页面**：`location.reload()` 方法可以重新加载当前页面。
    4. **替换当前历史记录**：`location.replace()` 方法可以用新的 URL 替换当前的历史记录条目，这意味着用户点击 “后退” 按钮时不会回到之前的页面。

### 窗口导航

- 风险：控制**新窗口**行为，实现跨上下文攻击。
- `window.open()` 是一个方法，用于在浏览器中打开一个新的窗口或标签页，并可以指定新窗口的 URL、大小、位置和其他特性。
- **与新窗口通信**：`window.open()` 会返回新创建窗口的 `window` 对象引用（在同源策略允许的情况下），使得父窗口可以与子窗口进行通信。

`const newWindow = window.open(url, windowName, [windowFeatures]);`

**常用场景**

- **OAuth 认证流程**：在第三方登录（如微信、QQ、GitHub 登录）时，通常会打开一个新窗口，用户在新窗口完成授权后，授权服务器会将令牌（Token）通过 URL 参数的形式传递回父窗口或回调页面。
- **展示详情或帮助页面**：点击 “查看详情” 或 “帮助” 按钮时，在新窗口打开一个包含详细内容的页面，而不影响当前页面的状态。
- **文件下载**：有时会通过 `window.open()` 打开一个指向文件的 URL，触发浏览器的下载行为。