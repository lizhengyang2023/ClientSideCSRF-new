# 客户端请求劫持攻击 - 步骤说明

## 📋 实验准备

### 1. 环境要求
- Node.js (推荐 v14+)
- 现代浏览器（Chrome、Firefox、Edge等）

### 2. 安装与启动

```bash
# 安装依赖
npm install

# 启动服务器
npm start
```

服务器将启动：
- 受害者服务器：http://127.0.0.1:3000
- 攻击者服务器：http://127.0.0.1:3001

## 🎯 实验步骤

### 第一步：打开受害者页面

1. 在浏览器中访问：http://127.0.0.1:3000/victim.html
2. 这是模拟的正常Web应用（如银行系统）
3. **重要：保持此页面打开，不要关闭**
4. 观察页面显示的账户信息：
   - 账户余额：¥1,000.00
   - 邮箱地址：user@example.com

### 第二步：打开攻击者页面

1. **在同一浏览器中**打开新标签页
2. 访问：http://127.0.0.1:3001/attacker.html
3. 这是恶意网站，包含各种攻击代码

### 第三步：观察攻击效果

在攻击者页面中，你将看到10种不同的攻击方式。对于每种攻击：

1. **查看代码**：点击"代码"标签页，查看攻击代码
2. **执行攻击**：点击"执行攻击"按钮
3. **查看结果**：点击"结果"标签页，查看攻击执行结果
4. **观察影响**：返回受害者页面，查看账户信息是否发生变化

---

## 🔍 详细攻击步骤说明

### 攻击1：表单自动提交攻击

**攻击原理：**
- 创建隐藏的HTML表单，包含恶意数据
- 页面加载时自动提交表单
- 浏览器自动携带Cookie发送POST请求

**实验步骤：**
1. 在攻击者页面找到"1. 表单自动提交攻击"
2. 点击"手动触发攻击"按钮
3. 观察结果：转账请求已发送到受害者服务器
4. 返回受害者页面，刷新查看余额是否减少

**预期结果：**
- ✅ 请求成功发送
- ✅ 即使没有用户交互，表单也会自动提交
- ✅ 转账请求被执行

**攻击影响：**
- 用户账户被自动转账500元到攻击者账户
- 用户完全不知情

---

### 攻击2：图片标签GET请求劫持

**攻击原理：**
- 使用img标签的src属性发送GET请求
- 浏览器尝试加载"图片"，实际发送了HTTP请求
- 即使图片加载失败，请求也已经发送

**实验步骤：**
1. 点击"2. 图片标签GET请求劫持"下的"执行攻击"按钮
2. 观察结果页面显示的信息
3. 打开浏览器开发者工具（F12），查看Network标签页
4. 可以看到对 `/api/user/action` 的GET请求

**预期结果：**
- ✅ GET请求已发送
- ✅ 请求携带了用户的Cookie
- ✅ 即使返回的不是图片，请求也成功执行

**攻击影响：**
- 可能触发用户操作（如删除账户）
- 可能泄露用户信息（通过URL参数）

---

### 攻击3：Fetch API请求劫持

**攻击原理：**
- 使用现代的Fetch API发送POST请求
- 设置 `credentials: 'include'` 携带Cookie
- 如果服务器CORS配置不当，可以成功执行

**实验步骤：**
1. 点击"3. Fetch API请求劫持"下的"执行攻击"按钮
2. 查看结果：显示收集到的数据
3. 打开开发者工具查看网络请求详情

**预期结果：**
- ✅ POST请求成功发送
- ✅ 用户数据被收集（UserAgent、Referer等）
- ✅ 服务器返回确认信息

**攻击影响：**
- 用户数据被恶意收集
- 敏感信息泄露（如果请求中包含Cookie等）

---

### 攻击4：sendBeacon API数据收集

**攻击原理：**
- sendBeacon专门用于页面卸载时发送数据
- 即使页面关闭，请求也会发送
- 优先级高于其他请求

**实验场景：**
- 用户行为追踪劫持
- 页面关闭时的数据收集

**实验步骤：**
1. 点击"4. sendBeacon API数据收集"下的"执行攻击"按钮
2. 观察结果：显示数据已发送
3. **尝试关闭攻击者页面**，观察数据是否仍然发送

**预期结果：**
- ✅ 数据立即发送成功
- ✅ 即使页面关闭，数据也会发送（需测试）
- ✅ 服务器收到数据

**攻击影响：**
- 用户行为被追踪
- 即使离开恶意网站，数据仍被收集

---

### 攻击5：JSONP回调劫持

**攻击原理：**
- 定义恶意回调函数
- 通过script标签加载JSONP端点
- 服务器返回数据并调用回调函数
- 回调函数获取敏感数据

**实验步骤：**
1. 点击"5. JSONP回调劫持"下的"执行攻击"按钮
2. 查看结果：显示劫持到的敏感数据
3. 观察数据包括：余额、邮箱、密钥等

**预期结果：**
- ✅ 成功劫持敏感数据
- ✅ 获取用户余额、邮箱等信息
- ✅ 甚至可以获取密钥等敏感信息

**攻击影响：**
- 用户敏感信息完全泄露
- 攻击者可以获取账户余额等机密数据

---

### 攻击6：iframe跨域请求劫持

**攻击原理：**
- 创建隐藏的iframe加载受害者页面
- 等待iframe加载完成
- 通过表单提交执行操作

**实验步骤：**
1. 点击"6. iframe跨域请求劫持"下的"执行攻击"按钮
2. 观察结果：显示邮箱更新请求已发送
3. 返回受害者页面，查看邮箱是否被修改

**预期结果：**
- ✅ 通过iframe成功提交表单
- ✅ 邮箱更新请求被执行
- ✅ 用户邮箱被恶意修改

**攻击影响：**
- 用户邮箱被修改为攻击者控制的邮箱
- 可能导致账户被完全接管

---

### 攻击7：XMLHttpRequest劫持

**攻击原理：**
- 使用传统的XHR发送AJAX请求
- 设置 `withCredentials: true` 携带Cookie
- 发送POST请求执行操作

**实验步骤：**
1. 点击"7. XMLHttpRequest劫持"下的"执行攻击"按钮
2. 查看结果：显示删除账户请求已发送
3. 观察请求是否成功（可能受到CORS限制）

**预期结果：**
- ✅ 请求已发送
- ⚠️ 可能受到CORS限制无法获取响应
- ✅ 但请求可能已经在服务器端执行

**攻击影响：**
- 可能删除用户账户
- 即使无法获取响应，操作也可能已执行

---

### 攻击8：Link标签CSS导入劫持

**攻击原理：**
- 使用link标签导入CSS资源
- 浏览器发送GET请求尝试加载资源
- 即使加载失败，请求也已经发送

**实验步骤：**
1. 点击"8. Link标签CSS导入劫持"下的"执行攻击"按钮
2. 查看结果：显示资源加载请求已发送
3. 打开开发者工具查看网络请求

**预期结果：**
- ✅ GET请求已发送
- ✅ 可以用于API端点探测
- ✅ 可能触发某些操作

**攻击影响：**
- 探测API端点
- 触发用户操作
- 泄露信息（通过URL）

---

### 攻击9：WebSocket连接劫持

**攻击原理：**
- 建立WebSocket连接发送消息
- 如果服务器未验证来源，连接成功
- 可以持续发送恶意消息

**应用场景：**
- 实时通信劫持
- 聊天系统攻击

**实验步骤：**
1. 点击"9. WebSocket连接劫持"下的"执行攻击"按钮
2. 查看结果：显示消息发送成功
3. 注意：这里用POST模拟WebSocket（实际环境需要WebSocket服务器）

**预期结果：**
- ✅ 消息发送成功
- ✅ 服务器接收消息
- ✅ 可以持续发送恶意数据

**攻击影响：**
- 劫持实时通信
- 发送恶意消息
- 可能影响聊天系统等

---

### 攻击10：EventSource/SSE劫持

**攻击原理：**
- 创建EventSource连接到SSE端点
- 建立持久连接接收服务器推送
- 即使数据只读，也可能泄露信息

**应用场景：**
- 实时数据流劫持
- 通知系统攻击

**实验步骤：**
1. 点击"10. EventSource/SSE劫持"下的"执行攻击说明"按钮
2. 查看说明：了解SSE攻击原理
3. 注意：需要服务器支持SSE（当前演示为说明）

**预期结果：**
- 📋 了解SSE攻击原理
- 📋 理解实时数据流劫持方式

**攻击影响：**
- 接收实时数据流
- 泄露敏感信息（即使是只读的）
- 劫持通知等实时信息

---

## 🔍 实验观察要点

### 1. 浏览器开发者工具

打开开发者工具（F12）观察：

- **Network标签页**：查看所有网络请求
  - 请求URL、方法、状态码
  - 请求头和响应头
  - 是否携带Cookie

- **Console标签页**：查看JavaScript输出
  - 攻击代码的执行日志
  - 错误信息（如果有）

- **Application标签页**：查看Cookie
  - 查看受害者页面的Cookie
  - 确认攻击是否携带Cookie

### 2. 服务器控制台

观察Node.js服务器控制台输出：
- 每个请求的日志
- 请求来源（Referer）
- 被劫持的操作类型

### 3. 受害者页面变化

返回受害者页面，观察：
- 账户余额是否变化
- 邮箱地址是否被修改
- 操作日志中的新记录

---

## 🛡️ 防护方法总结

### 1. CSRF Token
- 在表单中添加随机token
- 服务器验证token是否匹配

### 2. SameSite Cookie
- 设置Cookie的SameSite属性
- 防止跨站请求携带Cookie

### 3. 验证Referer/Origin
- 检查请求来源
- 拒绝非预期的来源

### 4. 使用POST代替GET
- 对状态修改操作使用POST
- 避免GET请求被轻易触发

### 5. 双重提交Cookie
- Cookie和请求体中的token必须匹配

---

## ⚠️ 重要提示

1. **仅用于教育目的**：本实验仅用于学习Web安全知识
2. **本地环境**：所有攻击都在本地环境进行，确保安全
3. **浏览器限制**：某些攻击可能受到浏览器安全策略限制
4. **CORS限制**：Fetch和XHR攻击可能受到CORS限制
5. **同源策略**：某些攻击可能无法直接获取响应，但请求可能已发送

---

## 📊 实验结果记录

建议记录以下内容：

1. **每种攻击的执行结果**
   - 是否成功
   - 是否受到限制
   - 实际效果

2. **浏览器行为观察**
   - 请求是否发送
   - 是否携带Cookie
   - 响应情况

3. **受害者页面变化**
   - 账户余额变化
   - 邮箱变化
   - 操作日志

4. **防护效果**
   - 哪些防护方法有效
   - 为什么有效
   - 如何绕过（如果有）

---

## 🎓 思考问题

1. 为什么这些攻击能够成功？
2. 浏览器的同源策略如何限制这些攻击？
3. 哪些攻击方式最危险？为什么？
4. 如何在实际项目中防止这些攻击？
5. 除了上述防护方法，还有哪些防御措施？

---

**祝实验顺利！如有问题，请查看README.md或源代码注释。**

